import React from 'react';
import '../components-styles/Footer.css'; // Asegúrate de tener el archivo CSS en la ubicación correcta

function Footer() {
    return (
        <header className="footer-container">
            
        </header>
    );
}

export default Footer;